# Fritz!Box Network Control Configuration

# Required: Fritz!Box Authentication
FRITZBOX_USER=your_username
FRITZBOX_PASSWORD=your_password

# Required: API Basic Authentication
AUTH_USER=admin
AUTH_PASSWORD=your_secure_password

# Optional: Fritz!Box Connection Settings
# Change these if your Fritz!Box uses different settings
FRITZBOX_IP=192.168.178.1
FRITZBOX_PORT=49000

# Optional: Default Target IP
# This IP will be used in the cron jobs if not overridden
TARGET_IP=192.168.178.50

# Optional: Timezone for cron jobs
# Examples: Europe/Berlin, America/New_York, Asia/Tokyo
TZ=Europe/Berlin